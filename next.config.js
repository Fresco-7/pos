/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['gdf.coth.com', "aceternity.com", "files.edgestore.dev", "cdn.devdojo.com"],
    },
}

module.exports = nextConfig
